# JassForge 更新日志

## ---- v1.0.1 ↓ ----
## 新增能力

### 语法与语言能力

- 新增 `//@ priority x n` 注解支持 作用为设置onInit函数的优先级 x为优先级 数字越大执行优先级越高 n为影响的行数 写到library上面
- 新增 `continue` 语句支持，覆盖循环内代码生成与语义校验。
- 新增 `stub` 方法支持，并补齐相关语义检查和代码生成测试。
- 新增 `module` 声明、实现与 mixin 代码生成支持。
- 新增 `interface` 体系：
  - 支持接口声明与实现检查。
  - 支持接口代码生成与运行期调度。
  - 支持接口继承、方法解析与结构多态相关能力。
- 新增语句级 debug 标志支持，改进 debug 代码生成定位能力。

### 初始化与工程能力

- 新增启动函数顺序管理。
- 新增初始化优先级注解支持，用于更稳定地处理全局初始化顺序。
- 更新 JassFrame 示例代码与 `Map.w3x`，覆盖新增 OOP、接口、模块相关能力。

## 修复与校验增强

### 生成后 JASS 校验

- 禁止 `code array`，避免生成非法或不稳定的 JASS。
- 增强最终生成 JASS 的合法性检查。
- 重构生成输出验证逻辑，新增或增强：
  - JASS 语法验证。
  - 调用签名与返回类型验证。
  - 闭包同步一致性检查。
  - 直接返回局部变量检测。
  - 无效双下标检测。
  - 无效数组赋值检测。
  - 数组用法不匹配检测。
  - 生成结构错误检测。

### 语义检查

- 增强全局变量可见性管理。
- 新增未定义变量检测。
- 修复接口声明中的可见性修饰符处理错误。
- 修复若干 JassFrame 示例和包内函数返回值类型不一致问题，将相关返回值统一到正确语义。

### 不支持语法诊断

- 明确移除并拒绝不支持的 `delegate` 语法。
- 明确移除并拒绝不支持的 `#include` 语法。
- 新增相关编译错误测试，避免不支持语法被静默接受。
- 现在明确禁止return real -> integer 比如返回值类型是real 那么只能返回0.0,禁止返回0 (因为jass中如果通过real直接返回0 integer的话会造成jass虚拟机中断执行 应该是个jass bug)

## 兼容性提示

- `delegate` 现在会被明确诊断为不支持语法。
- `#include` 现在会被明确诊断为不支持语法。
- `code array` 现在会被禁止。